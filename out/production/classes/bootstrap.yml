#Server port
server:
  port: 8888

#Git repo location
spring:
  cloud:
    config:
      server:
        git:
          uri: G:\support_prasanth\workspace\config-server\src\main\resources
          cloneOnStart: true

#Disable security of the Management endpoint
management:
  security:
    enabled: false